# 🚀 Instagram Group Moderator Bot

An advanced, adaptive-polling Python bot for Instagram Group Chats featuring Anti-Spam, Activity Tracking, and Auto-Follow back.

## ⚙️ Installation Guide

The core dependency for this bot is **`instagrapi`**. Depending on your system, the installation method slightly varies.

### 1️⃣ For Ubuntu / Debian / Linux Servers (Recommended)
This is the smoothest environment. No compilation errors will occur.
```bash
sudo apt update
sudo apt install python3 python3-pip
pip install -r requirements.txt
